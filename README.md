# seating-planner
Seating Planner


A simple seating planner


## How to run locally

### Start frontend
Run `cd frontend`
Run `npm run start`

### Start backend (jetty)
Run `cd backend`
Run `mvn exec:java`